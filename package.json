{
  "name": "innovation-sandbox-on-aws-costs",
  "version": "1.0.0",
  "description": "AWS cost reporter for Innovation Sandbox leases",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "build:lambda": "npm run build && node scripts/bundle-lambda.js",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["aws", "cost-explorer", "lambda", "sandbox"],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@aws-sdk/client-cost-explorer": "^3.962.0",
    "@aws-sdk/client-scheduler": "^3.962.0",
    "@aws-sdk/client-secrets-manager": "^3.962.0",
    "@aws-sdk/credential-providers": "^3.962.0",
    "commander": "^14.0.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^25.0.3",
    "esbuild": "^0.24.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
